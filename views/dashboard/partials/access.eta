<section id="access-section" class="section-card">
  <div class="section-header">
    <span class="bracket-prefix" style="display:inline-flex;align-items:center;"><%~ include('./icons', { name: 'check' }) %></span>
    <h2 class="heading-md">Access</h2>
  </div>

  <!-- Sub-section A: Change password (DSEC-01) -->
  <div style="max-width: 560px; margin-bottom: var(--spacing-8);">
    <h3 class="heading-md" style="margin-bottom: var(--spacing-3);">Change password</h3>

    <form
      hx-post="/dashboard/settings/password"
      hx-target="#content"
      hx-swap="innerHTML"
    >
      <input type="hidden" name="_csrf" value="<%= it.csrfToken %>">

      <!-- Current password -->
      <div class="field-row">
        <label for="currentPassword" class="body-md" style="display: block; margin-bottom: var(--spacing-1);">Current password</label>
        <input
          type="password"
          id="currentPassword"
          name="currentPassword"
          class="text-input"
          autocomplete="current-password"
          style="width: 100%;"
        >
      </div>

      <!-- New password -->
      <div class="field-row" style="margin-top: var(--spacing-3);">
        <label for="newPassword" class="body-md" style="display: block; margin-bottom: var(--spacing-1);">New password</label>
        <input
          type="password"
          id="newPassword"
          name="newPassword"
          class="text-input"
          autocomplete="new-password"
          style="width: 100%;"
        >
        <p class="caption-md" style="color: var(--color-text-muted); margin: var(--spacing-1) 0 0 0;">Minimum 12 characters.</p>
      </div>

      <!-- Confirm new password -->
      <div class="field-row" style="margin-top: var(--spacing-3);">
        <label for="confirmPassword" class="body-md" style="display: block; margin-bottom: var(--spacing-1);">Confirm new password</label>
        <input
          type="password"
          id="confirmPassword"
          name="confirmPassword"
          class="text-input"
          autocomplete="new-password"
          style="width: 100%;"
        >
      </div>

      <!-- Submit + flash -->
      <div style="margin-top: var(--spacing-3);">
        <button type="submit" class="btn btn-primary">Change password</button>
      </div>

      <% if (it.flash) { %>
      <%~ it.flash %>
      <% } %>
    </form>
  </div>

  <!-- Sub-section B: Domain (DSEC-02, DCFG-01) -->
  <div id="domain-subsection" style="max-width: 560px;">
    <h3 class="heading-md" style="margin-bottom: var(--spacing-3);">Domain</h3>

    <p class="body-md" style="margin-bottom: var(--spacing-3);">Set your domain to enable HTTPS. Leave blank to access via IP over HTTP only.</p>

    <form
      hx-post="/dashboard/settings/domain"
      hx-target="#content"
      hx-swap="innerHTML"
    >
      <input type="hidden" name="_csrf" value="<%= it.csrfToken %>">

      <!-- Domain input -->
      <div class="field-row">
        <label for="domain" class="body-md" style="display: block; margin-bottom: var(--spacing-1);">Domain</label>
        <input
          type="text"
          id="domain"
          name="domain"
          class="text-input"
          placeholder="example.com"
          value="<%= it.domain %>"
          autocomplete="off"
          style="width: 100%;"
        >
        <p class="caption-md" style="color: var(--color-text-muted); margin: var(--spacing-1) 0 0 0;">Do not include https:// or a trailing slash.</p>
      </div>

      <!-- Submit + flash -->
      <div style="margin-top: var(--spacing-3);">
        <button type="submit" class="btn btn-secondary">Save domain</button>
      </div>

      <% if (it.domainFlash) { %>
      <%~ it.domainFlash %>
      <% } %>
    </form>
  </div>
</section>
