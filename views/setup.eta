<div class="centered-card-wrapper">
  <div class="centered-card">
    <h1 class="display-xl">Set up Open Review</h1>
    <p class="body-md" style="color: var(--color-text-muted); margin-top: var(--spacing-2);">Create a password to protect the dashboard.</p>

    <% if (it.tokenValid) { %>
      <div style="margin-top: var(--spacing-24);">
        <form method="POST" action="/setup">
          <input type="hidden" name="_csrf" value="<%= it.csrfToken %>">
          <input type="hidden" name="token" value="<%= it.token %>">

          <div>
            <label for="password" class="body-md" style="display: block; margin-bottom: var(--spacing-1);">New password</label>
            <input type="password" id="password" name="password" class="text-input" autocomplete="new-password" required>
            <p class="caption-md" style="color: var(--color-text-muted); margin-top: var(--spacing-1);">Minimum 12 characters.</p>
          </div>

          <div style="margin-top: var(--spacing-3);">
            <label for="confirm" class="body-md" style="display: block; margin-bottom: var(--spacing-1);">Confirm password</label>
            <input type="password" id="confirm" name="confirm" class="text-input" autocomplete="new-password" required>
          </div>

          <div style="margin-top: var(--spacing-3);">
            <button type="submit" class="btn btn-primary" style="width: 100%;">Set password</button>
          </div>

          <% if (it.error) { %>
            <p class="flash-error" role="alert"><span style="display:inline-flex;align-items:center;flex-shrink:0;"><%~ include('./dashboard/partials/icons', { name: 'x' }) %></span><%= it.error %></p>
          <% } %>
        </form>
      </div>
    <% } else { %>
      <div style="margin-top: var(--spacing-24);">
        <p class="flash-error" role="alert"><span style="display:inline-flex;align-items:center;flex-shrink:0;"><%~ include('./dashboard/partials/icons', { name: 'x' }) %></span>This setup link is invalid or has already been used. Restart the service to generate a new one.</p>
      </div>
    <% } %>
  </div>
</div>
